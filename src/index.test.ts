describe('index', () => {
    describe('detectMonkeyPatches', () => {
        it('only checks known window functions', () => { })
        it('')
    })
    describe('fixMonkeyPatch', () => {
        it('throws an error if the prop is not a known window function', () => { })
        it("throws an error if the iframe can't be created", () => { })
    })
})